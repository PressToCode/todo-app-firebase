<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ContactFirebaseApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ContactFirebaseApp" parent="Base.Theme.ContactFirebaseApp" />

    <style name="Theme.ContactFirebaseApp.Splash" parent="Theme.SplashScreen">
        <!-- REMOVE icon and branding -->
        <item name="windowSplashScreenAnimatedIcon">@color/transparent</item>

        <!-- Set your custom background layout -->
        <item name="windowSplashScreenBackground">@color/beige</item>
        <item name="android:windowBackground">@color/beige</item>

        <item name="postSplashScreenTheme">@style/Theme.ContactFirebaseApp</item>
    </style>
</resources>